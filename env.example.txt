# Все переменные уже заданы в закоммиченном .env (тестовый ключ хакатона) — копировать ничего не нужно.
# Чтобы переопределить, раскомментируйте строку в .env.local (он имеет приоритет над .env).
# Не оставляйте переменную пустой без комментария: пустое значение в .env.local перекроет ключ из .env.

# OPENAI_API_KEY=sk-...                         # ключ OpenAI API
# OPENAI_MODEL=gpt-6-sol                      # модель анализа
# OPENAI_EMBEDDING_MODEL=text-embedding-3-large # модель эмбеддингов для сопоставления функций
